// Defining a function does NOT execute it. 
// Calling the function actually performs the specified actions with the indicated parameters.

function nameFunction( params) { // ()- here comes the type of parameters

// here comes some code{}
  return result // give it to the 
}

let finalResult = nameFunction()
// we call the function with the given parameters in the ()brackets

// --- BUILT - IN functions---

let string = "10"
let number = 10
let array = [1, 2, 3]

s.split(" ") // it divides a string  into an ordered list of substrings, puts them into an array, and returns the array
// ("") - here comes where to split the string

number.toString() // convert the number into a split

array.sort() // sorts the elements of an array, and returns the sorted array
// the default sort order is ascending

array.reverse() //reverses an array, the first element becomes the last, and the last element becomes the first.

array.join() //creates and returns a new string by concatenating all of the elements in an array
// () - here comes how to separate the elements - (), (""), (" "), ("-")

let numberFromString = parseInt(string) // returns an integer from a string
// () - here comes the variable which needed to be converted
