let myList = []
if (3 == 4) {
  myList.push("akarmi")
}
// myList is still empty

// if KEYWORD
// () - contains the condition (boolean)
// { } - contains 0 or more lines of code, it will run ONLY IF condition true

if (true) { // condition can be anything that is a boolean ( &&, ||, ==, !=, <, <=, ..., variables, values)

} else {
  // contains 0 or more lines of code, it will run ONLY IF condition true
}


switch (key) {
  case value: // if value == key...
     // do something 
    
    break; // until break

  default: // if no cases are matching
    break;
}